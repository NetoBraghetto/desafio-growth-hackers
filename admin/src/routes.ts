// import { views } from "views";

export const routes = {
  // customerList: {path: '/', title: `Clientes registrados | ${process.env.REACT_APP_NAME}`, Handler: views.List},
  // register: {path: '/cadastre-se', title: `Cadastre-se | ${process.env.REACT_APP_NAME}`, Handler: views.Register},
  // customerEdit: {path: '/clientes/:id/edit', title: `Cadastre-se | ${process.env.REACT_APP_NAME}`, Handler: views.Edit},
  // _404: {path: '*', title: 'Página não encontrada', Handler: views.Error404 },
};
